{
  "name": "galspanic",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server/server.js",
    "postinstall": "npm install --prefix server --production"
  },
  "engines": {
    "node": ">=18"
  }
}
