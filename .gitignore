# 환경변수 — 절대 커밋 금지
server/.env
.env

# 의존성
server/node_modules/
node_modules/

# 구매 데이터 (런타임 생성, 민감 정보)
server/data/purchases.json

# 로그
*.log
npm-debug.log*

# TWA Android 빌드 (키스토어 포함 — 별도 백업 필수)
app/
twa/

# Bubblewrap이 루트에 생성하는 Gradle 프로젝트 — 로컬 빌드 산출물/wrapper,
# 필요하면 `bubblewrap build`로 언제든 재생성 가능
.gradle/
build/
gradle/
gradlew
gradlew.bat
build.gradle
settings.gradle
local.properties
twa-manifest.json
manifest-checksum.txt

# 릴리즈 서명 비밀번호 포함 — 절대 커밋 금지, 별도 백업 필수
gradle.properties

# OS
.DS_Store
Thumbs.db
